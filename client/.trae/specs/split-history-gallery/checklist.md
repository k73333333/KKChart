- [x] 工作台（DashboardScreen）在初始进入时，图表画廊区域为空。
- [x] 工作台在生成新图表后，画廊区域能够正确展示当前最新生成的图表内容。
- [x] 验证生成的图表数据已被成功写入到本地 SQLite 数据库（`charts` 表）中。
- [x] 侧边栏导航成功添加了“历史记录”入口，且能正常跳转至 `HistoryScreen`。
- [x] `HistoryScreen` 能够从本地 SQLite 数据库中正确加载并展示所有持久化保存的历史图表数据。
- [x] 在 `HistoryScreen` 中进行删除操作，能够真正从本地数据库中移除记录并刷新列表。
- [x] 在 `HistoryScreen` 中进行复制操作，能够成功生成新副本记录并保存至数据库。